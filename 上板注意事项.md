# 当前结论

当前 `codex/v1-clean` 分支已经完成本地功能验证，可以进入板级工程集成阶段，但不能直接等同于已经上板验证通过。

当前本地工作分支：

```
branch: codex/v1-clean
remote: origin/codex/v1-clean
commit: 当前 `codex/v1-clean` HEAD
message: 在 teacher-sample 对齐基础上切到 packed radix-2 FFT butterfly fast path
```

本地已验证的范围：

- 汇编解释器对上老师 `FFT_input.coe` 和 `FFT_output.coe`。
- `mcu_v1_core_tb` 通过。
- `mcu_fft_system_tb` 通过，证明 `test_ROM[128..143] 16 槽信号输入 -> MCU radix-2 packed FFT -> verify_RAM 16 槽输出` 的本地 GHDL 流程正确。
- 当前 DSP fast path 的 host-side `timed_steps` 为 100；GHDL system TB 按 `cnt_start/cnt_stop` 口径量到 `cnt_cycles = 139`。
- 未运行 Vivado/xsim/综合/实现/bitstream/板卡/JTAG/XDC，因此没有 timing、资源、IP、引脚或板卡证据。

# 老师样例 I/O 顺序

当前实现以老师 `测试数据样例-2026` 为准，不再使用旧的 16 个 real/imag 交错输入假设。

`FFT_input.coe`：

```
slot 0..63     DFT 矩阵实部，Q7
slot 64..127   DFT 矩阵虚部，Q7
slot 128..135  原始信号实部，Q5
slot 136..143  原始信号虚部，Q5
```

`FFT_output.coe`：

```
slot 0..7      输出实部，Q12，自然序
slot 8..15     输出虚部，Q12，自然序
```

老师样例期望输出：

```
F280 E80A 1A80 FEA2 E080 16F6 E680 FA5E
E900 317C 2C00 1F8C 0B00 0C84 1600 D874
```

# 板级工程必须调整的接口

`origin/board-files-docs` 里的 `MCU_v1/BOARD_TOP/.../mcu_fft_system.vhd` 只有旧的 16 输入流程，不能直接用于 2026 老师样例。

必须修正为：

```
test_rom_addr      8-bit，至少能访问 slot 0..143
test_vector_in     16-bit signed
input_waddr        8-bit，能写入 core input_mem[0..143]
verify_ram_addr    6-bit，只需要写回 output slot 0..15
verify_vector_out  16-bit signed
```

当前本地已有可参考文件：

```
rtl/mcu_fft_system.vhd
tb/mcu_fft_system_tb.vhd
rtl/mcu_v1_core.vhd
rtl/mcu_v1_data_mem.vhd
```

# test_ROM 注意事项

上板工程中的 `test_ROM` 需要满足：

- 深度至少 144 个 16-bit word，当前 `BOARD_TOP` 配成 256 深度。
- 地址宽度至少 8 bit。
- 初始化内容使用老师 `FFT_input.coe`。
- 如果使用 Block Memory Generator，同步读延迟需要保留当前连续输入流里的首地址等待和提前发下一地址流程。
- 当前 DSP fast path 只需要加载 slot 128..143；slot 0..127 的 DFT 矩阵保留在 COE 中用于来源一致性，但 MCU 程序不再运行时读取。

# verify_RAM 注意事项

`verify_RAM` 只需要接收 16 个 16-bit 输出：

```
addr 0..7   实部
addr 8..15  虚部
```

检查时应使用 `FFT_output.coe` 的 16 个值逐槽比对。ILA 观察时只在 readback valid 对齐时判断地址和数据，不要只看裸 `verify_readback_data`。

# MCU core 接口注意事项

core 顶层应保持这些行为：

- reset 期间允许 `input_we` 写入 `input_mem`。
- `input_mem` 不应被 reset 清空。
- `work_mem` 和 `output_mem` 可以在 reset 时清零。
- 程序读输入区时，16-bit 输入必须符号扩展为 32-bit。
- 程序写输出区时，只保存 `write_data[15:0]`。
- `output_rdata` 当前按组合读假设。如果改成同步读，需要在 `mcu_fft_system` 输出 dump 状态机里增加等待拍。
- 正常结束应是 `halted_debug = 1` 且 `illegal_debug = 0`。

# 汇编和地址映射注意事项

当前汇编不是旧的 radix-2 DIF `FFT/8` 版本，也不是上一版逐输出 direct DFT。
它针对老师样例固定 `dftmtx(8)` 的 Q7 系数，使用 bit-reversed input 的
radix-2 DIT packed FFT butterfly：

```
real[k] = sum_n xr[n] * wr[k,n] - xi[n] * wi[k,n]
imag[k] = sum_n xr[n] * wi[k,n] + xi[n] * wr[k,n]
```

定点格式：

```
Q5 input * Q7 matrix = Q12 output
```

当前关键地址：

```
input slots 128..143 -> byte address slot * 4
output real write   -> 0x800..0x81c
output imag write   -> 0x820..0x83c
```

`mcu_v1_data_mem` 会把 `0x800..0x83c` 映射到外部 `output_mem[0..15]`。
程序不再读取 `input_mem[0..127]` 的 DFT 矩阵槽；矩阵系数已经写死在
机器码算法中。当前程序会把每个复数样本打包成 `low16=real, high16=imag`
的 Q12 packed complex 值，再用 `SADD16/SSUB16/SSAX/SMUAD/SMUSD` 完成
三层 radix-2 butterfly，并用两条 `STMIA` 批量写回实部和虚部。

当前本地计数：

```
host checker timed_steps = 100
GHDL mcu_fft_system_tb cnt_cycles = 139
```

# 上板前检查清单

代码和工程集成：

- 使用 `codex/v1-clean` 的 RTL、汇编 `.mem` 和系统包装层改动。
- 确认板级工程的 `mcu_fft_system` 已从旧 16 输入改成读取 `FFT_input.coe[128..143]` 并写入 `input_mem[128..143]`。
- 确认 `mcu_v1_core.input_waddr` 和 `mcu_v1_data_mem.input_waddr` 是 8 bit。
- 确认 `test_ROM` COE 是老师 `FFT_input.coe`。
- 确认 `verify_RAM` 读回地址 0..15 对应老师 `FFT_output.coe`。

仿真和实现：

- 先跑本地或工程级功能仿真，确认 16 个输出完全匹配。
- 再综合、实现、生成 bitstream。
- 看 timing summary，不能有 timing violation。
- 上板后用 ILA 或 verify_RAM 读回确认输出。

# 当前风险

- 当前本地 GHDL 通过不代表 Vivado IP 配置和板级工程已经同步。
- `origin/board-files-docs` 旧接口文档仍写 16 输入和 6-bit `input_waddr`，这是和 2026 样例不匹配的地方。
- 如果板级 `test_ROM` 仍是 64 深度或 6-bit 地址，系统无法寻址老师 COE 的 signal window `128..143`。
- 如果 `output_rdata` 在板级被实现成同步读 RAM，必须补等待拍，否则 verify_RAM 可能写入错位数据。

# 相关页面

接口说明： 接口说明

本页记录的是 2026 老师样例适配后的上板注意事项。旧接口说明中的 16 输入口径只能作为历史背景，不能覆盖老师样例。
